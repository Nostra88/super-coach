{
  "name": "supercoach-backend",
  "version": "1.0.0",
  "description": "SUPERCOACH Backend",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "jest --verbose",
    "test:cov": "jest --coverage",
    "backtest": "node test/backtest.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "@neondatabase/serverless": "^0.10.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "jest": "^29.0.0"
  }
}